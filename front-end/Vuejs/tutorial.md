1.set up environment
```shell
#install npm & nodejs
# install yarn
sudo npm install --global yarn
# check yarn version
yarn -v 
# get into project
cd project
# initial project
yarn

# lunch app
yarn serve
```